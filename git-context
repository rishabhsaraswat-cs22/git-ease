#!/usr/bin/env bash
# git-context: Show current Git branch and status.

branch=$(git rev-parse --abbrev-ref HEAD 2>/dev/null)

if [ $? -ne 0 ]; then
  echo "Not a git repository."
  exit 1
fi

echo "📌 Current branch: $branch"
echo
git status -s

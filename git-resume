#!/usr/bin/env bash
# git-resume: Resume work after a git-save.

if git log -1 --pretty=%B | grep -q "WIP: saving progress"; then
  echo "Resuming from last WIP commit..."
  git reset --soft HEAD~1
  echo "✅ Changes restored to working directory."
else
  echo "⚠️ No WIP commit found to resume."
fi
